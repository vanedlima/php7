<?php
				$frase= "hoje é sexta";
				$q= strpos($frase, "sexta");
				echo $q;

 				//vai me da como resultado :   8
 				// mostra em que posição do array encontra-se a palavra sexta, ou seja, na posição 8.

				echo "<br/>";
				
				$texto= substr ($frase,0,$q);
				var_dump($q);
				
				// retorna int(8)
				
				$a="hoje";
				$texto2= substr($frase,$q + strlen($a), strlen($frase));
				var_dump($a);
				//strlen — Retorna o tamanho de uma string
				
?>
